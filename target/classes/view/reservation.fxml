<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ListView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.control.Label?>

<?import javafx.scene.image.Image?>
<BorderPane xmlns="http://javafx.com/javafx/8" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.eventcraft.controller.ReservationSalleController">

    <!-- Top Bar -->
    <top>
        <HBox fx:id="menuBar" spacing="20" alignment="CENTER_LEFT"
              style="-fx-padding: 10; -fx-background-color: #CBA979;">
            <Button fx:id="backButton" onAction="#handleBackButton"
                    style="-fx-background-color: transparent; -fx-border-color: transparent;">
                <graphic>
                    <ImageView fitWidth="24" fitHeight="24" preserveRatio="true">
                        <image>
                            <Image url="@/images/arrow-left.png" />
                        </image>
                    </ImageView>
                </graphic>
            </Button>
            <!-- Espace flexible pour pousser les boutons à droite -->
            <Region HBox.hgrow="ALWAYS" />

            <!-- Boutons à droite -->
            <Button fx:id="afficherReservationsButton" text="Mes Réservations"
                    onAction="#handleAfficherReservationsClick"
                    style="-fx-border-color: #FFFFFF; -fx-text-fill: white; -fx-font-size: 14px;
                           -fx-border-width: 1px; -fx-padding: 8 16; -fx-background-color: transparent" />

            <Button text="Déconnexion" fx:id="logoutBtn" onAction="#handleLogout"
                    style="-fx-border-color: #FFFFFF; -fx-text-fill: white; -fx-font-size: 14px;
                           -fx-border-width: 1px; -fx-padding: 8 16; -fx-background-color: transparent;" />
            <Button fx:id="chatButton" text="Chat AI" onAction="#openChatWindow" style=" ;-fx-text-fill: white;-fx-background-color: #8D598F;"/>

        </HBox>
    </top>

    <!-- Left Side Navigation -->
    <left>
        <VBox fx:id="sidenav" prefWidth="180" minWidth="180" maxWidth="180"
              style="-fx-background-color: #CBA979; -fx-padding: 15px;">

            <!-- Logo -->
            <VBox alignment="TOP_CENTER" style="-fx-padding: 10;">
                <ImageView fx:id="logoImageView" fitWidth="100" fitHeight="100" preserveRatio="true" />
            </VBox>

            <!-- Navigation Buttons -->
            <Button text="Évènements" onAction="#handleAccueilButton"
                    style="-fx-text-fill: white; -fx-font-size: 16px;
               -fx-background-color: transparent;
               -fx-border-color: transparent transparent white transparent;
               -fx-border-width: 0 0 1px 0;
               -fx-padding: 10px;" />

            <Button text="Mes Salles"  style="-fx-text-fill: white; -fx-font-size: 16px;
               -fx-background-color: transparent;
               -fx-border-color: transparent transparent white transparent;
               -fx-border-width: 0 0 1px 0;
               -fx-padding: 10px;" onAction="#handleSallesNavigation"/>

            <Button text="Contact" onAction="#handlecontactButton"
                    style="-fx-text-fill: white; -fx-font-size: 16px;
               -fx-background-color: transparent;
               -fx-border-color: transparent transparent white transparent;
               -fx-border-width: 0 0 1px 0;
               -fx-padding: 10px;" />
        </VBox>
    </left>

    <!-- Contenu principal au centre -->
    <center>
        <VBox fx:id="mainContent" spacing="10" alignment="CENTER"
              style="-fx-padding: 20; -fx-background-color: #FFFFFF;">

            <Label text="Réservez votre salle en Tunisie"
                   style="-fx-font-size: 24px; -fx-font-weight: bold;
                          -fx-padding: 20px; -fx-text-fill: #98A695;"/>

            <ListView fx:id="listViewReservations" prefHeight="400" VBox.vgrow="ALWAYS"
                      style="-fx-background-color: white;
                             -fx-padding: 10px;" />
        </VBox>
    </center>
</BorderPane>
